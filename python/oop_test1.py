class Car:
    def __init__(self, make, model, color, price );
        self.make = make
        self.model = model
        self.color = color
        self.price = price

    def my_print(self):
        print(self.make)

class ElectricCar(Car):
    def __inf__(self, make, model, color, price, batterySize):
                super().__init__(make, model, color, price)
                self.batterySize = batterySize

    def main():
          
          my_car = ElectricCar('Tesala', 'Model S', s )